<h1>All Episodes</h1>

<ul>
  <% @episodes.each do |episode| %>
    <li><%= "Episode #{episode.number} - #{episode.date.strftime("%B %d, %Y")}"%></li>
    <%= link_to [episode.number, episode.date], episode_path(episode) %>
  <% end %>
</ul>
